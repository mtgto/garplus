{
  "name": "garplus",
  "version": "0.0.2",
  "description": "ガルーンをちょっと使いやすくするツール",
  "dependencies": {},
  "devDependencies": {
    "@types/chrome": "0.0.35",
    "webpack": "^1.13.3"
  },
  "scripts": {
    "build": "npm run build:preprocess && npm run build:typescript && npm run build:webpack && npm run build:manifest",
    "build:preprocess": "node scripts/consts-template.js > scripts/consts.ts",
    "build:typescript": "tsc --removeComments scripts/*.ts",
    "build:webpack": "./node_modules/.bin/webpack --optimize-minimize",
    "build:manifest": "node manifest-template.js > dist/manifest.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mtgto/garplus.git"
  },
  "author": "mtgto",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/mtgto/garplus/issues"
  },
  "homepage": "https://github.com/mtgto/garplus#readme"
}
